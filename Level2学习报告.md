# Level 2 学习报告 · 管理员模式与库存管理

> 配套代码：`project-level2.cpp`（逐行注释版）
> 完成日期：2026-09-22 ｜ 项目：七一一便利店 POS 系统

---

## 一、这次做了什么

| 任务 | 内容 | 状态 |
|---|---|---|
| **2.1 管理员模式** | `admin` 密码进入（提示符变 `admin>`）、`setprice` 改价、`itemadd` 上架、`itemdel` 下架、`back` 退出、`setpwd` 改密码 | ✅ |
| **2.2 库存管理** | `restock` 进货（+=）、`setstock` 盘点（=）、`prices` 增加库存列、结账自动扣库存、超库存扫入拦截 | ✅ |
| **架构升级①** | 商品表从"硬编码"升级为 **`items.csv` 文件驱动**——改价/上架/下架重启后不再丢失 | ✅ |
| **架构升级②** | 新增**模式状态机**：收银员模式（`> `）↔ 管理员模式（`admin>`） | ✅ |

---

## 二、文件体系一览（本项目的四个数据文件）

| 文件 | 存什么 | 打开模式 | 为什么 |
|---|---|---|---|
| `items.csv` | **当前**商品表（条码,名称,价格,库存） | **覆盖写** | 它是"当前状态"：改完就是新的全量状态 |
| `sales.csv` | **历史**销售流水（day,No,time,name,qty,amount） | **追加写** `ios::app` | 它是"只增不改"的账本，绝不能覆盖 |
| `day.txt` | 今天是第几天（一个数字） | 覆盖写 | 只关心"现在的值" |
| `password.txt` | 管理员密码 | 覆盖写 | 只关心"现在的值" |

> 💡 **核心区分**：**状态类文件用覆盖，流水类文件用追加**。判断标准："我只关心最新值" → 覆盖；"每一条都不能丢" → 追加。

---

## 三、新增/修改的函数一览

| 函数 | 作用 | 关键点 |
|---|---|---|
| `save_items()` | 商品表写回 items.csv | 覆盖写 + `fixed/setprecision(2)` |
| `load_items()` | 启动加载商品表 | 没文件 → 默认商品 + 立刻落盘 |
| `load_pwd()` / `save_pwd()` | 密码读写 | `fin >> p` 读一个词即可 |
| `print_price()`（改） | 商品表打印 | 增加了 Stock 列 |
| `flush_sale()` / `print_sales()`（沿用） | 销售报表 | 1.3 的"缓存一笔"方案不变 |
| `main()` 中新增分支 | admin / back / setprice / itemadd / itemdel / restock / setstock / setpwd | 每个分支都先做**权限检查** |

---

## 四、关键实现讲解

### 4.1 商品表持久化：为什么这是 Level 2 的地基？

Level 1 里商品是写死在 `initialization()` 里的。但如果 `setprice` 改完价、重启程序又变回去了，管理员功能就是个笑话。所以第一步必须让商品表**住进文件**：

```cpp
ifstream fin("items.csv");
if(!fin) {                       // 没有文件 = 首次运行
    塞入默认三件套;
    save_items();                // 立刻写盘（从此以文件为唯一真相）
}
else { 逐行读 → 拆 4 列 → atof/atoi 转换 → 填进 objects[] }
```

**任何修改商品的命令（setprice/itemadd/itemdel/restock/setstock/结账扣库存）后都要调用 `save_items()`**——这叫"写穿"（write-through）：内存和文件永远一致。

### 4.2 管理员模式：一个布尔值 + 一个提示符

```cpp
bool isAdmin = false;                        // 全局模式标志
cout << (isAdmin ? "admin> " : "> ");        // 提示符随模式切换（三目运算符）
```

`admin` 命令用 **`getline(cin, pwd)` 读"下一行"** 作为密码（而不是 `iss >>`，因为密码不该按空格拆词）。密码对 → `isAdmin = true`。

**为什么用"模式"而不是每条管理命令都输密码？** —— 符合真实收银场景：管理员登录一次，连续操作多件事，`back` 退出。这也是"会话"思想的雏形。

### 4.3 itemdel 的陷阱：商品表和购物车必须"同进退" ⭐

购物车是**按下标**和商品表对应的（`shop_basket[1]` 就是 `objects[1]` 的购买记录）。删除商品时如果只移动商品表：

```
删除前：objects = [Cola, Lollipop, Noodles]   shop_basket = [Cola买2, 咖啡买1, ...]
删除 Cola 后（只移商品表）：objects = [Lollipop, Noodles]   但 shop_basket[1] 还记着"Cola 的 2 件"
→ 结账时 shop_basket[1] 被当成 Lollipop 的数量 → 张冠李戴、算错账！
```

**正确做法：两个数组同步前移**：

```cpp
for(int i = idx;i < itemCount;i++)
{
    objects[i]     = objects[i + 1];      // 商品表前移
    shop_basket[i] = shop_basket[i + 1];  // 购物车同步前移（删掉的那件自然被顶掉）
}
shop_basket[itemCount] = Node2();         // 清理尾部残留
```

### 4.4 库存的两个决策

**① 拦截时机：扫入时拦**（而不是结账时）——顾客想买第 3 件但只剩 2 件时，当场提示，不让他白扫。

**② 扣减时机：结账成功后统一扣**——如果"加购即扣"，顾客中途 `drop` 或走人，库存就漏扣了；结账时一次性扣，语义是"只有真正卖出去的才减库存"。

---

## 五、设计决策表（⭐ 面试与复盘用）

| 决策 | 选择 | 理由 |
|---|---|---|
| 历史小票是否随改价变化 | **不变（快照）** | 存的是成交时的名称+金额，不是引用；营业额必须按成交价算 |
| 删除商品时购物车 | **同步平移** | 下标耦合，不处理会串数据 |
| 库存不足 | 扫入时拦截 | 即时反馈，体验好 |
| 扣库存时机 | 结账成功后 | 中途放弃不产生脏扣减 |
| 新商品初始库存 | 0 | 还没进货就是 0，走 restock 流程 |
| 密码存储 | 明文（学习版） | 教学简化；真实系统必须哈希+盐（见面试题 6） |
| 权限控制 | 命令级检查 | 每个管理命令先查 `isAdmin`，收银员模式给出提示 |

---

## 六、用到的函数/头文件清单

| 头文件 | 函数/类型 | 用途 |
|---|---|---|
| `<fstream>` | `ifstream` / `ofstream` / `ios::app` | 四个数据文件的读写 |
| `<sstream>` | `istringstream` + `getline(iss, f, ',')` | 拆词（命令参数）、拆列（CSV） |
| `<iomanip>` | `setw` / `left` / `right` / `fixed` / `setprecision` | 表格对齐、金额两位小数 |
| `<ctime>` | `time` / `localtime` / `strftime` | 取当前时间 "HH:MM:SS" |
| `<cstdlib>` | `atoi` / `atof` | 字符串 → 整数/小数（不抛异常，失败返回 0） |
| `<string>` | `string` / `substr` / `c_str` / `to_string` | 字符串处理 |

---

## 七、测试记录（六个场景全过）

| # | 场景 | 结果 |
|---|---|---|
| 1 | 2.1 样张全流程（admin → setprice → itemadd → itemdel → prices → back） | ✅ 输出与样张一致 |
| 2 | 2.2 库存命令（restock +10 → 80；setstock 5 → 5） | ✅ |
| 3 | 库存拦截（库存 2 扫第 3 件被拦）+ 结账后库存 2→0 | ✅ |
| 4 | 改密码后：旧密码被拒、新密码可登、重启后仍生效 | ✅ |
| 5 | **购物车里有 Cola 时删除 Cola**：小票只剩 Lollipop（平移正确） | ✅ |
| 6 | 1.x 全回归（扫码/结账/sales 报表） | ✅ 未破坏老功能 |

---

## 八、面试可能遇到的问题（重点）

> 每题给出【考察点】和【回答要点】。建议先自己试着答一遍再看要点。

### Q1. 为什么商品改价后，历史小票不能跟着变？你的系统靠什么保证的？

- 【考察点】快照 vs 引用、数据设计
- 【要点】sales.csv 存的是成交时的**名称和金额**（快照），不是商品编号的引用；改价只影响之后的交易。如果小票只存编号、显示时查最新价，历史金额就会失真、营业额会算错。

### Q2. 删除商品时，为什么购物车要跟着一起移动？不动会怎样？

- 【考察点】数据结构的下标耦合、边界思维
- 【要点】购物车按下标对应商品表；商品表前移后下标含义变了，购物车不同步就会"张冠李戴"——结账把 A 的数量算到 B 头上。做法：两个数组同步前移 + 清尾部残留。

### Q3. 如果购物车改存"条码字符串"而不是下标，设计上有什么不同？

- 【考察点】设计权衡
- 【要点】以条码为键就不怕下标错位；代价是每次操作多一次查找，且商品被删后车里的条目要么清除要么标记失效。两种都可行，关键是保证"引用一致性"。

### Q4. 库存扣减为什么放在结账时，而不是加购时就扣？

- 【考察点】状态变更时机、一致性
- 【要点】加购即扣的话，顾客中途放弃（drop/走人）就漏扣了，还得配套恢复逻辑；结账统一扣 = "只有卖出去的才减"，一次变更、语义清晰。

### Q5. 两个收银台同时卖最后一件商品会发生什么？怎么解决？

- 【考察点】并发/超卖意识
- 【要点】本系统是单机单用户，天然无并发；多终端下会"超卖"。真实方案：数据库行锁/事务，或原子条件更新（`UPDATE items SET stock=stock-1 WHERE code=? AND stock>=1`），凭受影响行数判断是否成功。

### Q6. 密码明文存在 password.txt 有什么问题？真实系统怎么做？

- 【考察点】安全基础
- 【要点】明文一旦泄露直接失守。真实系统存**哈希**（bcrypt/scrypt/argon2）+ 每个用户随机盐，登录比对哈希；传输走 HTTPS；再配错误次数限制防爆破。学习项目用明文是为了简单，要有这个意识。

### Q7. `ios::app` 和 `ofstream` 默认模式有什么区别？你的四个文件分别用哪种、为什么？

- 【考察点】文件模式
- 【要点】`app` 追加不覆盖；默认截断清空再写。sales.csv 是流水 → 追加；items.csv / day.txt / password.txt 是"当前状态" → 覆盖。

### Q8. `'1' - '0'` 可以，为什么 `"1" - '0'` 不行？

- 【考察点】类型系统
- 【要点】`'1'` 是 char（本质是整数，'1'=49、'0'=48，相减得 1）；`"1"` 是字符串（类对象/字符数组），没有减法运算符。字符串转数字用 atoi/stod。

### Q9. atoi 和 stoi 有什么区别？你选了哪个？

- 【考察点】标准库熟练度
- 【要点】atoi 是 C 风格：失败返回 0、**不抛异常**；stoi 是 C++ 风格：失败**抛异常**（更严格，但要 try/catch）。本项目的文件数据是自己写的，用 atoi 更简单容错。

### Q10. sales 报表里"金额打在一笔的最后一行"，实现的关键难点是什么？

- 【考察点】前瞻问题、流式处理
- 【要点】读到第一行时不知道这笔后面还有没有行（前瞻问题）。解法：**缓存当前一笔**，等"下一行 No. 变了"或"文件读完"才输出；注意最后一笔要在循环外补打。

### Q11. 让你加"退货/整单撤销"，你会怎么设计？

- 【考察点】扩展性、账本思维
- 【要点】不要改动/删除历史记录，而是**追加**一条退货记录（负数金额或标记类型）；库存回补；报表按符号合并。金融系统千百年来的原则：账本只追加，不改写。

### Q12. 数组 vs vector，你的项目为什么用数组？什么时候该换？

- 【考察点】数据结构选型
- 【要点】商品数量少、有固定上限（MAX=100），数组简单直接；若需要频繁增删、动态扩容（成百上千商品），`vector` 的 `push_back/erase` 更安全省心，还免去手写平移。

### Q13. 你的程序里全局变量很多，利弊是什么？

- 【考察点】代码组织意识
- 【要点】小项目里直观省事；变大后状态难追踪、容易互相干扰、函数间耦合强。进阶做法：用 `struct/class` 把"数据 + 操作它的函数"封装在一起（比如 `Store` 类管理商品表），这就是之后面向对象课程的内容。

### Q14. 你是怎么测试这个系统的？你认为最重要的边界用例是哪些？

- 【考察点】测试意识
- 【要点】样张逐字对照 + 重启持久化 + 一致性场景（删商品时车里有它、库存为 0 还扫、密码输错、文件被删/被手改）。测试思路：**正常流 + 边界 + 异常**。

### Q15. `itemadd` 时为什么要检查"条码已存在"和"数组是否满"？

- 【考察点】防御性编程
- 【要点】条码是主键，重复会导致查找歧义（`findItem` 永远只会找到第一个）；数组满则越界（未定义行为）。所有"外部输入"进入系统前都要校验——这是写工程代码和写作业的最大区别。

---

## 九、下一步

- Level 3 自由发挥方向：打折/组合销售（改价体系）、多日报表、终端图形化统计
- 代码改进 backlog：购物车改按条码索引、`vector` 化、密码哈希、单元测试
